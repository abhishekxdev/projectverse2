export default async () => {
  console.log('\n Test suite completed. Cleaning up...');
  // Note: Emulator data is ephemeral - it's cleared when emulators stop
  //! If you need explicit cleanup, you can call clearEmulatorData() here
};
